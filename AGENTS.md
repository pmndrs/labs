# @pmndrs/labs

- **Type:** standalone project
- **Package Manager:** pnpm
- **Linter:** oxlint
- **Formatter:** prettier

## Commands

- `pnpm test` — run tests
- `pnpm build` — build
- `pnpm lint` and `pnpm format` — run before committing
